services:
  - type: web
    name: whatsapp-gemini-bot
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: D360_API_KEY
        sync: false
